import { DropdownHelperDirective } from './dropdown-helper.directive';

describe('DropdownHelperDirective', () => {
  it('should create an instance', () => {
    const directive = new DropdownHelperDirective();
    expect(directive).toBeTruthy();
  });
});
